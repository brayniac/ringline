# GENERATED by experiments/recv-buffer-sweep/generate.py — do not hand-edit.
# Phase A, chunk 5: 6 arms, one guest boot.
# Design: docs/recv-buffer-geometry-sweep-design.md  (#416)
name = "ringline recv-buffer sweep phase-a chunk-5"

[params]
bins_artifact = "01a0b0ac-b17c-73a2-6ea5-44592cdc2caa"
image         = "spool/images/debian-13-base-20260912.1@golden"
proxy_ip      = "172.31.0.1"
load_ip       = "172.31.0.2"
port          = "7878"
drain_port    = "7900"
workers       = "8"
warmup        = "5"
duration      = "20"

# ── server under test: hv02, alone on the box ──────────────────────────────

[[jobs]]
name = "server"
tags = ["z1.baremetal"]

[[jobs.steps]]
type = "anvil-vm"
mode = "start"
shape = "z1.c"
image = "{image}"
gpu = false
data_address = "{proxy_ip}/24"
boot_timeout = 600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
inputs = [{ artifact = "{bins_artifact}", name = "bins.tgz", dest = "/home/anvil" }]
payload = '''
set -uo pipefail
cd /home/anvil && tar xzf bins.tgz && chmod +x out/*
sudo sysctl -w net.core.somaxconn=65535 net.ipv4.tcp_max_syn_backlog=65535 \
  net.core.rmem_max=134217728 net.core.wmem_max=134217728
./out/bench-server-uring --print-backend
'''

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf65536-ring64-c64-start"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
metrics = true
upload = true
payload = '''
set -uo pipefail
mkdir -p /home/anvil/results
# Foreground under `timeout -s TERM`: SIGTERM is the graceful shutdown, so the
# arm dumps its counters and the step ends on its own. `|| true` because
# `timeout` exits 124 on the deadline it is supposed to hit.
timeout -s TERM 45 /home/anvil/out/bench-server-uring \
  --runtime ringline --addr {proxy_ip}:{port} --workers {workers} \
  --proxy-backend {load_ip}:{drain_port} --proxy-api conn --msg-size 16384 --recv-buffer-bytes 65536 --recv-ring-size 64 \
  --metrics-out /home/anvil/results/forward-stream-buf65536-ring64-c64.metrics.json \
  > /home/anvil/results/forward-stream-buf65536-ring64-c64.server.log 2>&1 || true
tail -3 /home/anvil/results/forward-stream-buf65536-ring64-c64.server.log
'''

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf65536-ring64-c64-done"
timeout = 3600

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf65536-ring256-c64-start"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
metrics = true
upload = true
payload = '''
set -uo pipefail
mkdir -p /home/anvil/results
# Foreground under `timeout -s TERM`: SIGTERM is the graceful shutdown, so the
# arm dumps its counters and the step ends on its own. `|| true` because
# `timeout` exits 124 on the deadline it is supposed to hit.
timeout -s TERM 45 /home/anvil/out/bench-server-uring \
  --runtime ringline --addr {proxy_ip}:{port} --workers {workers} \
  --proxy-backend {load_ip}:{drain_port} --proxy-api conn --msg-size 16384 --recv-buffer-bytes 65536 --recv-ring-size 256 \
  --metrics-out /home/anvil/results/forward-stream-buf65536-ring256-c64.metrics.json \
  > /home/anvil/results/forward-stream-buf65536-ring256-c64.server.log 2>&1 || true
tail -3 /home/anvil/results/forward-stream-buf65536-ring256-c64.server.log
'''

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf65536-ring256-c64-done"
timeout = 3600

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf262144-ring16-c64-start"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
metrics = true
upload = true
payload = '''
set -uo pipefail
mkdir -p /home/anvil/results
# Foreground under `timeout -s TERM`: SIGTERM is the graceful shutdown, so the
# arm dumps its counters and the step ends on its own. `|| true` because
# `timeout` exits 124 on the deadline it is supposed to hit.
timeout -s TERM 45 /home/anvil/out/bench-server-uring \
  --runtime ringline --addr {proxy_ip}:{port} --workers {workers} \
  --proxy-backend {load_ip}:{drain_port} --proxy-api conn --msg-size 16384 --recv-buffer-bytes 262144 --recv-ring-size 16 \
  --metrics-out /home/anvil/results/forward-stream-buf262144-ring16-c64.metrics.json \
  > /home/anvil/results/forward-stream-buf262144-ring16-c64.server.log 2>&1 || true
tail -3 /home/anvil/results/forward-stream-buf262144-ring16-c64.server.log
'''

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf262144-ring16-c64-done"
timeout = 3600

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf262144-ring256-c64-start"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
metrics = true
upload = true
payload = '''
set -uo pipefail
mkdir -p /home/anvil/results
# Foreground under `timeout -s TERM`: SIGTERM is the graceful shutdown, so the
# arm dumps its counters and the step ends on its own. `|| true` because
# `timeout` exits 124 on the deadline it is supposed to hit.
timeout -s TERM 45 /home/anvil/out/bench-server-uring \
  --runtime ringline --addr {proxy_ip}:{port} --workers {workers} \
  --proxy-backend {load_ip}:{drain_port} --proxy-api conn --msg-size 16384 --recv-buffer-bytes 262144 --recv-ring-size 256 \
  --metrics-out /home/anvil/results/forward-stream-buf262144-ring256-c64.metrics.json \
  > /home/anvil/results/forward-stream-buf262144-ring256-c64.server.log 2>&1 || true
tail -3 /home/anvil/results/forward-stream-buf262144-ring256-c64.server.log
'''

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf262144-ring256-c64-done"
timeout = 3600

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf1048576-ring4-c64-start"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
metrics = true
upload = true
payload = '''
set -uo pipefail
mkdir -p /home/anvil/results
# Foreground under `timeout -s TERM`: SIGTERM is the graceful shutdown, so the
# arm dumps its counters and the step ends on its own. `|| true` because
# `timeout` exits 124 on the deadline it is supposed to hit.
timeout -s TERM 45 /home/anvil/out/bench-server-uring \
  --runtime ringline --addr {proxy_ip}:{port} --workers {workers} \
  --proxy-backend {load_ip}:{drain_port} --proxy-api conn --msg-size 16384 --recv-buffer-bytes 1048576 --recv-ring-size 4 \
  --metrics-out /home/anvil/results/forward-stream-buf1048576-ring4-c64.metrics.json \
  > /home/anvil/results/forward-stream-buf1048576-ring4-c64.server.log 2>&1 || true
tail -3 /home/anvil/results/forward-stream-buf1048576-ring4-c64.server.log
'''

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf1048576-ring4-c64-done"
timeout = 3600

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf1048576-ring256-c64-start"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
metrics = true
upload = true
payload = '''
set -uo pipefail
mkdir -p /home/anvil/results
# Foreground under `timeout -s TERM`: SIGTERM is the graceful shutdown, so the
# arm dumps its counters and the step ends on its own. `|| true` because
# `timeout` exits 124 on the deadline it is supposed to hit.
timeout -s TERM 45 /home/anvil/out/bench-server-uring \
  --runtime ringline --addr {proxy_ip}:{port} --workers {workers} \
  --proxy-backend {load_ip}:{drain_port} --proxy-api conn --msg-size 16384 --recv-buffer-bytes 1048576 --recv-ring-size 256 \
  --metrics-out /home/anvil/results/forward-stream-buf1048576-ring256-c64.metrics.json \
  > /home/anvil/results/forward-stream-buf1048576-ring256-c64.server.log 2>&1 || true
tail -3 /home/anvil/results/forward-stream-buf1048576-ring256-c64.server.log
'''

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf1048576-ring256-c64-done"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
upload = true
artifacts = ["/home/anvil/server-results.tgz"]
payload = '''
tar czf /home/anvil/server-results.tgz -C /home/anvil results
'''

[[jobs.steps]]
type = "anvil-vm"
mode = "stop"
upload = true

# ── load generator and sink: hv01 ──────────────────────────────────────────

[[jobs]]
name = "load"
tags = ["z2.baremetal"]

[[jobs.steps]]
type = "anvil-vm"
mode = "start"
shape = "z2.c"
image = "{image}"
gpu = false
data_address = "{load_ip}/24"
boot_timeout = 600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
inputs = [{ artifact = "{bins_artifact}", name = "bins.tgz", dest = "/home/anvil" }]
payload = '''
set -uo pipefail
cd /home/anvil && tar xzf bins.tgz && chmod +x out/*
sudo sysctl -w net.core.rmem_max=134217728 net.core.wmem_max=134217728 \
  net.ipv4.ip_local_port_range="1024 65535"
'''

# The sink for every forward arm, up for the whole chunk.
[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
detach = true
unit = "proxy-drain"
payload = "exec /home/anvil/out/proxy-drain --addr 0.0.0.0:{drain_port}"

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf65536-ring64-c64-start"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
payload = '''
set -uo pipefail
cd /home/anvil && mkdir -p results
for i in $(seq 1 60); do
  (exec 3<>/dev/tcp/{proxy_ip}/{port}) 2>/dev/null && { exec 3>&-; break; }
  sleep 1
done
ulimit -n 500000
./out/proxy-load --addr {proxy_ip}:{port} --clients 64 --threads 16 --msg-size 16384 --warmup {warmup} --duration {duration} > results/forward-stream-buf65536-ring64-c64.json 2> results/forward-stream-buf65536-ring64-c64.log || true
echo "== forward-stream-buf65536-ring64-c64"; cat results/forward-stream-buf65536-ring64-c64.json
'''

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf65536-ring64-c64-done"
timeout = 3600

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf65536-ring256-c64-start"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
payload = '''
set -uo pipefail
cd /home/anvil && mkdir -p results
for i in $(seq 1 60); do
  (exec 3<>/dev/tcp/{proxy_ip}/{port}) 2>/dev/null && { exec 3>&-; break; }
  sleep 1
done
ulimit -n 500000
./out/proxy-load --addr {proxy_ip}:{port} --clients 64 --threads 16 --msg-size 16384 --warmup {warmup} --duration {duration} > results/forward-stream-buf65536-ring256-c64.json 2> results/forward-stream-buf65536-ring256-c64.log || true
echo "== forward-stream-buf65536-ring256-c64"; cat results/forward-stream-buf65536-ring256-c64.json
'''

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf65536-ring256-c64-done"
timeout = 3600

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf262144-ring16-c64-start"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
payload = '''
set -uo pipefail
cd /home/anvil && mkdir -p results
for i in $(seq 1 60); do
  (exec 3<>/dev/tcp/{proxy_ip}/{port}) 2>/dev/null && { exec 3>&-; break; }
  sleep 1
done
ulimit -n 500000
./out/proxy-load --addr {proxy_ip}:{port} --clients 64 --threads 16 --msg-size 16384 --warmup {warmup} --duration {duration} > results/forward-stream-buf262144-ring16-c64.json 2> results/forward-stream-buf262144-ring16-c64.log || true
echo "== forward-stream-buf262144-ring16-c64"; cat results/forward-stream-buf262144-ring16-c64.json
'''

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf262144-ring16-c64-done"
timeout = 3600

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf262144-ring256-c64-start"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
payload = '''
set -uo pipefail
cd /home/anvil && mkdir -p results
for i in $(seq 1 60); do
  (exec 3<>/dev/tcp/{proxy_ip}/{port}) 2>/dev/null && { exec 3>&-; break; }
  sleep 1
done
ulimit -n 500000
./out/proxy-load --addr {proxy_ip}:{port} --clients 64 --threads 16 --msg-size 16384 --warmup {warmup} --duration {duration} > results/forward-stream-buf262144-ring256-c64.json 2> results/forward-stream-buf262144-ring256-c64.log || true
echo "== forward-stream-buf262144-ring256-c64"; cat results/forward-stream-buf262144-ring256-c64.json
'''

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf262144-ring256-c64-done"
timeout = 3600

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf1048576-ring4-c64-start"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
payload = '''
set -uo pipefail
cd /home/anvil && mkdir -p results
for i in $(seq 1 60); do
  (exec 3<>/dev/tcp/{proxy_ip}/{port}) 2>/dev/null && { exec 3>&-; break; }
  sleep 1
done
ulimit -n 500000
./out/proxy-load --addr {proxy_ip}:{port} --clients 64 --threads 16 --msg-size 16384 --warmup {warmup} --duration {duration} > results/forward-stream-buf1048576-ring4-c64.json 2> results/forward-stream-buf1048576-ring4-c64.log || true
echo "== forward-stream-buf1048576-ring4-c64"; cat results/forward-stream-buf1048576-ring4-c64.json
'''

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf1048576-ring4-c64-done"
timeout = 3600

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf1048576-ring256-c64-start"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
payload = '''
set -uo pipefail
cd /home/anvil && mkdir -p results
for i in $(seq 1 60); do
  (exec 3<>/dev/tcp/{proxy_ip}/{port}) 2>/dev/null && { exec 3>&-; break; }
  sleep 1
done
ulimit -n 500000
./out/proxy-load --addr {proxy_ip}:{port} --clients 64 --threads 16 --msg-size 16384 --warmup {warmup} --duration {duration} > results/forward-stream-buf1048576-ring256-c64.json 2> results/forward-stream-buf1048576-ring256-c64.log || true
echo "== forward-stream-buf1048576-ring256-c64"; cat results/forward-stream-buf1048576-ring256-c64.json
'''

[[jobs.steps]]
type = "barrier"
name = "forward-stream-buf1048576-ring256-c64-done"
timeout = 3600

[[jobs.steps]]
type = "anvil-vm"
mode = "exec"
upload = true
artifacts = ["/home/anvil/client-results.tgz"]
payload = '''
tar czf /home/anvil/client-results.tgz -C /home/anvil results
'''

[[jobs.steps]]
type = "anvil-vm"
mode = "stop"
upload = true
